import 'package:flutter/material.dart';

class AnotherOsScreen extends StatelessWidget {
  const AnotherOsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Run on iOS'),
      ),
    );
  }
}
